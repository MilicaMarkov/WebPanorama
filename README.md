# WebPanoramaProject

To run React application, execute the following commands:

cd my-app
npm install
npm start


To run React application, execute the following commands:

cd api
npm install
npm start
